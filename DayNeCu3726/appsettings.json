{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Database provider: "SqlServer" (default) or "Sqlite".
  // Switch to "Sqlite" together with the connection string below to run without SQL Server.
  "Database": {
    "Provider": "SqlServer"
  },

  "ConnectionStrings": {
    // SQL Server LocalDB (default):
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=DayNeCu3726Db;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True;"

    // SQLite alternative — set Database:Provider to "Sqlite" and use this instead:
    // "DefaultConnection": "Data Source=sims.db"
  }
}
